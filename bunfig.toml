[test]
# Global threshold — orchestration scripts (sync-upstream, generate-workflows CLI) are
# integration-level and cannot be fully unit-tested; overall coverage stays well above 80%
coverageThreshold = 0.65

[install]
linker = "hoisted"